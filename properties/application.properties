app.version=trial version 201909125
app.developers= Lorenzo Rodriguez (lrodriguez71@gmail.com)
app.licence=MIT (https://opensource.org/licenses/MIT)
# Set here configurations for the database connection
spring.datasource.url=jdbc:mariadb://localhost:3306/dbwsft
spring.datasource.username=wsft
spring.datasource.password=uwsft2019
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1
# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application
spring.jpa.hibernate.ddl-auto=update
# Naming strategy
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyHbmImpl
spring.jpa.hibernate.naming.physical-strategy=org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dial.ect=org.hibernate.dialect.MySQL5InnoDBDialect
private.key=WSFTB.key
security.signing-key=SigNingKey*2019
security.jwt.client-id=WsftClient
security.jwt.client-secret=WSFTB.2$19
security.jwt.grant-type=password
security.jwt.scope-read=read
security.jwt.scope-write=write
path.public.key=SECREPT_PATH/Keys/public-key.perm
path.private.key=SECREPT_PATH/Keys/private-key.perm